<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:scrollbars="none">

    <LinearLayout
        android:id="@+id/zlView"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/ll_profile_login"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <include layout="@layout/layout_mode_change_activity" />

            <include layout="@layout/layout_profile_my_userinfo" />

        </LinearLayout>

        <Button
            android:id="@+id/btn_my_switch_login"
            style="@style/layout_button_full_white"
            android:layout_marginBottom="@dimen/space_list_top_withoutwave"
            android:layout_marginTop="@dimen/dp_20"
            android:text="切换账号" />
    </LinearLayout>

</ScrollView>