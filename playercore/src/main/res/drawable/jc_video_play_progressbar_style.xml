<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 设置背景色（黑色） -->

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="0dip" />

            <gradient
                android:endColor="@color/black_e"
                android:startColor="@color/black_e" />
        </shape>
    </item>

    <!-- 设置进度条颜色（绿色） -->

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="0dip" />

                <gradient
                    android:endColor="@color/red_bt"
                    android:startColor="@color/red_bt" />
            </shape>
        </clip>
    </item>

</layer-list>
